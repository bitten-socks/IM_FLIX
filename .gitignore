# Build output / dependencies
node_modules/
dist/
.vercel/

# Vendored KMK checkout + the CircuitPython firmware it belonged to.
# Superseded by QMK/flix_vibe6; kept locally for reference only.
MCU/

# Firmware build artifacts
*.uf2

# Bring-up screenshots (Mu console, device manager). Schematic captures worth
# keeping should be moved into docs/ and committed deliberately.
화면 캡처 *.png
Untitled.png

# OS noise
.DS_Store
Thumbs.db
desktop.ini
